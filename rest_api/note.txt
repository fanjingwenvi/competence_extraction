source venv/bin/activate 
## activate venv
pip install -r requirements.txt 
## intall the packages 
hypercorn main:app 
## run the api 
http://127.0.0.1:8000/docs

## venv
python3 -m venv venv
pip install neo4j
pip install python-dotenv
pip install fastapi
pip install hypercorn
pip freeze > requirements.txt

Ctrl + Shift + P  Terminal: Create New Integrated Terminal
python.terminal.activateEnvironment": true



from ## set up linux 

apt update && apt upgrade -y  ## latest version of the packages, security  
apt install  -y -q build-essential python3-pip python-dev ## python 
pip3 install -U pip setuptools wheel ## pip 
pip3 install gunicorn uvloop httptools 

cp .requirements.txt /app/requirements.txt ## copy to linux folder app

pip3 intall -r /app/requirements.txt

## copy service 

## 
## workers: web form 
## run method 

## setup.sh 
## admin: run the software on the docker 
## linux command to Docker command 